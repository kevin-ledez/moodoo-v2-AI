{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter un lien de navigation{% endblock %}

{% block admin_content %}
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold text-gray-800">Ajouter un lien de navigation</h1>
        <a href="{{ path('admin_navigation_link_index') }}" class="btn btn-outline">
            <i class="fas fa-arrow-left mr-2"></i>
            Retour à la liste
        </a>
    </div>

    <div class="card bg-white shadow rounded-lg p-6">
        {{ include('admin/navigation_link/_form.html.twig') }}
    </div>
{% endblock %}
